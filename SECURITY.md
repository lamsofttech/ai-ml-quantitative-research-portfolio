# Security policy

Do not commit API keys, access tokens, credentials, private datasets, or personally identifiable information. Use environment variables and provide a safe `.env.example` when configuration is required. Report a suspected vulnerability privately through GitHub Security Advisories rather than a public issue.

